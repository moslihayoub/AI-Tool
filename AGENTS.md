# System Instructions
When tasked to do ANY changes, bugs, additions, or features, AI Agent must:
- Update the `components/InfraView.tsx` `currentLogs` with the action you took (even if the user doesn't ask for it).
- This is a strict user requirement.
